import React, { Component } from "react";
import { StyleSheet } from "react-native";
import Colors from "../../Colors";

const smsStyles = StyleSheet.create({
    body: {
    }
});

export default smsStyles;
